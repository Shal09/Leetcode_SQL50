SELECT sell_date, count(DISTINCT product) as num_sold,
GROUP_CONCAT(DISTINCT product ORDER BY product) AS products
FROM Activities
GROUP BY sell_date



NOTE:
The GROUP_CONCAT(DISTINCT product ORDER BY product) AS products statement in SQL is used to concatenate distinct product names into a single string while maintaining a specified order.
